# Supabase Configuration
# Get these values from: https://supabase.com/dashboard/project/_/settings/api

# Public URL (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Public anon key (safe to expose, protected by RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ⚠️ SECURITY WARNING:
# DO NOT add SUPABASE_SERVICE_ROLE_KEY here or in .env.local
# The service role key bypasses ALL Row-Level Security (RLS) policies
# Only use it in server-side functions if absolutely necessary

# Database URL (for running migrations and seed scripts)
# Format: postgresql://postgres:[password]@[host]:[port]/postgres
# DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# Optional: Node Environment
# NODE_ENV=development
